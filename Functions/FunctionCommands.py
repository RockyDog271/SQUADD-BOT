# IF msg = >> saves

    # read save amt from json

# IF msg = >> report

    # process report and send to channels

# IF msg = >> ticket

    # process ticket and send to channels

# IF msg = >> suggest

    # process suggestion and send to channels

# ELSE

    # send unknown command error